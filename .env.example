# Environment Variables Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these values from: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Your Supabase anonymous/public key (safe to expose in client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Supabase Service Role Key (server-side only, never expose to client)
# Only needed if you want to bypass RLS policies
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# CLERK AUTHENTICATION
# =============================================================================
# Get these values from: https://dashboard.clerk.com

# Clerk Publishable Key (safe to expose in client)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx

# Clerk Secret Key (server-side only, never expose to client)
CLERK_SECRET_KEY=sk_test_xxxxx

# Clerk URL Configuration
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/login
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/signup
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# =============================================================================
# GOOGLE GEMINI AI CONFIGURATION
# =============================================================================
# Get your API key from: https://aistudio.google.com/app/apikey

# Gemini API Key (safe to expose in client for this use case)
NEXT_PUBLIC_GEMINI_API_KEY=your-gemini-api-key

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. This project uses CLERK AUTHENTICATION
#    - Google OAuth is handled by Clerk
#    - Configure OAuth providers in Clerk Dashboard
#    - No need for separate Google OAuth credentials
#
# 2. All NEXT_PUBLIC_* variables are exposed to the browser
#    - Safe for API keys that are meant to be public (Clerk publishable, Supabase anon key)
#    - Never put sensitive keys (secret keys) in NEXT_PUBLIC_* variables
#
# 3. After creating .env file:
#    - Restart your dev server (npm run dev)
#    - Verify variables load correctly in browser console
#
# 4. For production:
#    - Set these variables in your hosting platform (Vercel, Netlify, etc.)
#    - Never commit .env file to version control
